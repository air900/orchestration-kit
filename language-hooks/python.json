{
  "PostToolUse": [
    {
      "matcher": "Edit",
      "hooks": [
        {
          "type": "command",
          "command": "case \"$CLAUDE_FILE_PATHS\" in *.py) cd {{PROJECT_PATH}} && ruff check --fix \"$CLAUDE_FILE_PATHS\" 2>&1 | head -30 || true ;; esac"
        }
      ]
    },
    {
      "matcher": "Write|MultiEdit",
      "hooks": [
        {
          "type": "command",
          "command": "jq -r '.tool_input.file_path // empty' | { read f; if [ -n \"$f\" ] && echo \"$f\" | grep -qE '\\.py$'; then ruff format \"$f\" 2>/dev/null || true; fi; }",
          "timeout": 10000
        }
      ]
    }
  ]
}
