{
  "PostToolUse": [
    {
      "matcher": "Edit",
      "hooks": [
        {
          "type": "command",
          "command": "case \"$CLAUDE_FILE_PATHS\" in *.rs) cd {{PROJECT_PATH}} && cargo check 2>&1 | head -30 || true ;; esac"
        }
      ]
    },
    {
      "matcher": "Write|MultiEdit",
      "hooks": [
        {
          "type": "command",
          "command": "jq -r '.tool_input.file_path // empty' | { read f; if [ -n \"$f\" ] && echo \"$f\" | grep -qE '\\.rs$'; then rustfmt \"$f\" 2>/dev/null || true; fi; }",
          "timeout": 10000
        }
      ]
    }
  ]
}
